import { CommandContext, CommandDefinition } from "@rbxts/cmdr";

const Vizualize: CommandDefinition = {
	Name: "Vizualize",
	Aliases: ["vz"],
	Description: "Vizualize Voxels",
	Group: "DefaultAdmin",
	Args: [],

	// ClientRun: (context: CommandContext) => {
		
	// }
}

export = Vizualize